pub mod models;
use models::print;
use models::strings;
use models::functions;
use models::types;
use models::arrays;


fn main() {
    //Hello world in rust
    // println!("Hello, world!");

    // //Print from an another file!
    // print::run();
    // strings::run();
    // functions::run();
    // types::run();
    arrays::run();


}
