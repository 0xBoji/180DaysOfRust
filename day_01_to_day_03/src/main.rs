pub mod models;
use models::print;
use models::strings;
use models::functions;
use models::types;
use models::arrays;
use models::vars;
use models::cli;
use models::loops;
use models::vectors;
use models::structs;
use models::conditionals;
use models::enums;
use models::pointer_ref;


fn main() {
    //Hello world in rust
    // println!("Hello, world!");

    // //Print from an another file!
    // print::run();
    // strings::run();
    // functions::run();
    // types::run();
    // arrays::run();
    // vars::run();
    // cli::run();
    // loops::run();
    // vectors::run();
    // structs::run();
    // conditionals::run();
    // enums::run();
    // pointer_ref::run();

}
